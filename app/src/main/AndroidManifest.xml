<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lazo.controlvehicularcsjpiu">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- android:name="android.permission.WRITE_EXTERNAL_STORAGE" -->
    <application
        android:name=".bd.FirebaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".forms.FormRegUsuarios" />
        <activity android:name=".forms.FormRegVehiculos" />
        <activity android:name=".utils.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".forms.FormRegSalida" />
        <activity android:name=".forms.ListUsuariosActivity" />
        <activity android:name=".forms.FormRegParametros" />
        <activity
            android:name=".forms.FormVehiculos"
            android:label="" />

        <activity android:name=".forms.ListVehiculosActivity"></activity>
    </application>

</manifest>